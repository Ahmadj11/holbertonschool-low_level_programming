#inlcude "main.h"
/**
 * main - main function
 * @argc: count arguments
 * @argv: array of command arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);
	return (0);
}
