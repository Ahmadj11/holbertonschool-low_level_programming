#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for upper or lowercase letter s 
 *
 * Return: 1 if uppercase 0 if lowercase 
 */
int _isupper(int c);
{
	char c;
	if (c = 'A')
{
	printf("%c: %d\n", c, _isupper(c));
}
return (0)
}
